# 小瓜 · 喂饭陪伴 Agent 运行依赖
#
# 文本对话链路仅依赖 Python 3.9+ 标准库,不装任何第三方包也能启动
# (server.py / agent.py / llm.py 默认用系统 curl 或标准库 urllib 发请求)。
#
#   certifi —— llm.py 在 curl 失败、回退到 urllib 时,用它提供更完整的
#              CA 证书,缓解个别机器上偶发的 SSL 握手问题;缺失则退回系统默认证书。
#
# 语音入口(唤醒词 + 豆包流式 ASR)需要下面这组依赖;不装则用
# MEALMATE_VOICE_ENABLED=0 关闭语音,文本对话不受影响。
#
# 安装:  pip install -r requirements.txt
certifi>=2023.7.22

# ---- 语音入口依赖 ----
numpy>=1.26,<3
sherpa-onnx>=1.10.30,<2
volcengine-audio==0.2.4
websockets>=15,<16

# ---- 机器人语音输出依赖(可选;配了 HEYRICE_ROBOT_TTS_URL 才需要)----
# 另需系统安装 ffmpeg。需 Python 3.9+(edge-tts 7.x 的令牌算法要求较新环境)。
edge-tts>=7,<8
